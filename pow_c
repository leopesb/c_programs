#include <stdio.h>
#include <math.h>

int main(void) {

    for(int exp = 5; exp > 0; exp--) {
        int x = pow(2,exp);
        printf("2 to the power of %d is %d\n", exp, x);
    }
    return 0;
}
